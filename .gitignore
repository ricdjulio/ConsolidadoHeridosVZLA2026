# === SECRETOS: NUNCA subir la API key ===
api,txt
api.txt
*.key
.env
.env.*
credentials.json

# === Entorno virtual de Python ===
venv/
.venv/
env/

# === Cache de Python ===
__pycache__/
*.pyc
*.pyo

# === Archivos locales / temporales ===
.DS_Store
heridos.xlsx
*.tmp

# === Artefactos generados por consolidar.py ===
# Se versionan en el repo (es el "Consolidado" que se comparte).
# IMPORTANTE: solo el COORDINADOR los regenera y commitea, para evitar conflictos.
