# === SECRETOS: NUNCA subir la API key ===
api,txt
api.txt
*.key
.env
.env.*
credentials.json

# === Entorno virtual de Python ===
venv/
.venv/
env/

# === Cache de Python ===
__pycache__/
*.pyc
*.pyo

# === Archivos locales / temporales ===
.DS_Store
heridos.xlsx
*.tmp

# =====================================================================
# DATOS DE PACIENTES — NUNCA se suben al repo público (info sensible)
# Se comparten por un canal privado (Drive/grupo cerrado), no por GitHub.
# =====================================================================
datos/*.csv
!datos/.gitkeep
heridos_consolidado.xlsx
heridos_consolidado.csv
